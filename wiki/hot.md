<!-- ============================================================= -->
<!-- AUTO-GENERATED by wiki/update_hot.py - DO NOT HAND-EDIT.       -->
<!-- Your edits WILL be overwritten on the next run. The only       -->
<!-- editable region is the "Next Actions" block below, which the    -->
<!-- generator preserves across regenerations.                       -->
<!-- ============================================================= -->

# Hot - current state

_Last generated: 2026-07-29 by update_hot.py._

## Current State

- Experiments on record: **8** ("⚠️: 1, "✅: 1, marginal-positive: 1, no-edge: 2, partial: 1, risk-mgmt-only: 1, weak: 1). See [index.md](index.md).
- Project pivot in progress: from single-asset **direction** (no edge, AUC ~ 0.51-0.52)
  toward **cross-sectional / breadth** strategies (IR ~ IC*sqrt(N)). See [lessons.md](lessons.md).

## Recent Activity

## [2026-07-29] build | Enabled LIVE order sending in edgelab/live (user: Pepperstone is a DEMO, no risk). Set config_live.yaml live_trading:true. Hardened the previously-untested live path in broker.py: (1) DEMO-ACCOUNT SAFETY GATE — connect() reads account_info().trade_mode and REFUSES to send orders if the account is not DEMO unless allow_real_account:true (default false) -> pointing at a real account later fails loudly instead of trading real money; (2) _filling() picks a filling mode the symbol supports (IOC/FOK/RETURN via SYMBOL_FILLING_MODE bitmask) instead of hardcoded IOC (broker-reject risk); (3) _enforce_stops() widens SL/TP to clear the broker's min stop distance (trade_stops_level) else order_send rejects; (4) _clamp_lots() caps to volume_min/max; (5) _send_retry() refreshes price + retries on requote/price-off; (6) SL/TP rounded to symbol digits; (7) trade journal CSV (edgelab/live/_out/trades.csv) records every entry/exit in BOTH dry-run and live. Verified offline: build with live_trading:true OK (build doesn't connect), _enforce_stops/_clamp_lots/demo-guard unit-tested, dry-run smoke test still reproduces brick1 short (sl 128.45 vs 128.625) + brick3 long. Live order path itself can't be run in this env (needs the terminal) -> user runs on their machine w/ Algo Trading enabled. Recommend --once first to confirm connection + account shows DEMO, then the loop. NEXT: run on Pepperstone demo, watch trades.csv, and re-validate brick3 on Pepperstone crypto bars.
## [2026-07-29] BUGFIX(live) | First real --status against PepperstoneUK-Demo (login 62133658, DEMO, balance 10036.10) connected fine + demo-guard passed. BUT caught a DANGEROUS sizing mismatch: config initial_balance=100000 while the real demo is ~10k -> 1R = 1%*100000 = 1000 = ~10% of the actual account per trade. Fix: added size_from_account:true (default) — after connect, the runner rebases risk.initial_balance (and the daily/DD floors) on the ACTUAL account balance. Verified: 1R 1000 -> 100.36 (true 1%), daily floor 9534.30 (-5%), DD floor 9032.49 (-10%). Set false only for a real fixed-size challenge where the nominal size is known. runner._sync_account_size() called after connect in status/--once/loop. Lesson: never size off a config number that can mismatch the live account.
## [2026-07-29] experiment | Brick 3 RE-VALIDATED on Pepperstone crypto D1 (the live data source; user noted it was originally profitable on Pepperstone before switching to Yahoo for deeper history). Pepperstone cache is actually deep: BTC 2016-01+ (3136 bars), ETH 2018-06+ (2679). Same MACD-RSI signal/engine/barriers on Pepperstone BTC+ETH 2018-07+: +16.7 R/yr PF1.55 maxDD9.1R t=3.93 corr-0.02 9/9yrs PASS (realistic cost BTC6/ETH16 bps). COST-ROBUST: pessimistic 8/20 bps -> +16.2 R/yr t=3.80 (MACD-RSI is low-turnover, holds multi-day, so spread barely bites). Real Pepperstone spread: BTC ~5.6 bps (modelled 5, ok), ETH ~15.8 bps (modelled 8 = 2x understated) BUT recent 2023+ tighter (BTC 2.6, ETH 12.6). vs Yahoo same window +22.4 R/yr -> Pepperstone ~30% lower, NOT from cost but shorter history (BTC 2016 vs 2014) + different broker daily-close time (780 trades vs 1104). CONCLUSION: brick 3 edge holds on the data we actually trade; live book expectation ~+30 R/yr (crypto +16.7 not +24) instead of +38 on Yahoo. The live runner already pulls Pepperstone bars for brick 3 -> no code change, caveat resolved. Updated system.md, exp-008. Tool: scratchpad/brick3_pepperstone.py.
## [2026-07-29] live+BUGFIX | Runner went live on the demo: first trade = BTCUSD SHORT 0.03 lots @63955.72 SL67242.54 TP59025.49 (ticket 81405353). VERIFIED CORRECT: (a) signal — MACD-RSI on the last CLOSED Pepperstone bar (28-Jul) = -1, a fresh flip from +1 (27-Jul), so backtest would also enter short 29-Jul; (b) sizing — 0.03 BTC * 3286.8 SL-dist = ~$98.6 ~ 1R (100.36) [resolves the "first ticket ~1R" check]; (c) barriers RR = 4930/3287 = 1.5. BUT found + fixed a latent bug: CryptoMacdStrategy computed the signal INCLUDING today's still-forming D1 bar (intraday-evolving) instead of the last CLOSED bar. Fixed: brick3 now pulls get_bars_raw, drops the forming bar (last-bar broker-date == server-today) and decides on closed bars (daily), keeping the forming bar (daily_all) only for current price + dry-run SL/TP touch. Verified on mock: decides -1 on 28-Jul close (MATCH). Also fixed pd.Timestamp.utcnow() deprecation -> now(tz='UTC'). Restart is SAFE: open_position reads MT5 by magic (103) so the runner adopts the existing BTC short, no double-entry. (Brick 1 uses shift(1) D1 ATR + closed M1, brick 2 uses shift(1) ATR -> both already closed-bar-correct.) NEXT: restart runner to load the fix; watch trades.csv.
## [2026-07-29] BUGFIX(sizing) | User caught: ETHUSD LONG 0.82 lots risked ~86 EUR not 100 (=1R) — account is EUR but ETH/BTC are USD-denominated, and my tick_value-based lots_for() mis-converts on a cross-ccy account (over-estimated loss/lot -> under-sized ~14%). FIX: added Broker.lots_for_risk(logical,direction,entry,sl,risk_budget) that uses MT5 order_calc_profit (native instrument->deposit-ccy conversion) to size so the entry->SL loss == 1R in EUR exactly; tick-value formula kept only as offline/dry-run fallback. LiveRiskManager.risk_budget()=risk%*initial. All 3 strategies now call broker.lots_for_risk (was risk.lots_for). Verified compile + wiring + fallback. Under-sizing was SAFE (conservative) but imprecise -> now exact. Existing ETH/BTC positions kept (slightly under-1R); only NEW entries get exact sizing. Restart to load.
## [2026-07-29] build | Made the live runner run-forever robust (user: "fais ce qui est mieux"). (1) In-process resilience: broker.healthy()+reconnect() — the loop health-checks MT5 each cycle and re-initializes on a dropped connection, then re-syncs account size; per-strategy try/except already isolates brick failures. (2) Rotating file log edgelab/live/_out/runner.log (5MB x5). (3) Clean exit codes: 42 = account blew the prop DD (supervisor won't restart), 0 = clean/interrupt. (4) Supervisor run_forever.ps1 — relaunches the runner on CRASH, stops on 42/0 or a _out/STOP file; logs _out/supervisor.log. (5) install_task.ps1 — registers a Scheduled Task 'edgelab-live' to auto-start the supervisor at logon (no admin). SL/TP are broker-side so risk stays capped even if the runner is down; only time-exits + new entries need it alive. Note headless restart needs the MT5 terminal to auto-login (leave running or set mt5_path + save credentials). Best = VPS. Compile + PS1 syntax verified. Updated README + codebase-map.
## [2026-07-29] build | VPS multi-terminal support (user has a Contabo VPS running another algo on a MetaQuotes terminal; wants this Pepperstone portfolio alongside). MetaTrader5 python = one terminal per process, chosen by path -> the two algos coexist as separate processes each with its own mt5_path. Added to broker.connect(): mt5_path/mt5_login/mt5_password/mt5_server/mt5_portable passthrough to mt5.initialize(), + SAFETY GATE expect_server (server name must contain e.g. 'Pepperstone' else disconnect+refuse) so the runner can NEVER fire orders into the wrong (MetaQuotes) account if it attaches to the wrong terminal. config_live.yaml gained the block (expect_server: Pepperstone default). Verified: guard refuses on 'MetaQuotes-Demo', allows 'PepperstoneUK-Demo'. Live runner needs NO parquet caches (pulls bars from the terminal); only verify.py needs them. README got a full VPS deploy section (install 2nd Pepperstone terminal, pip deps, set mt5_path+expect_server, --status to confirm server=Pepperstone, run under run_forever.ps1/install_task.ps1).
## [2026-07-28] FROZEN SYSTEM | Final 3-brick portfolio locked. BRICK1 = NAS100 US-open ATR-breakout, low-vol regime (edgelab/intraday/atr_breakout.py, real M1, PF1.27). BRICK2 = XAUUSD turn-of-month, SL=1.5*ATR (edgelab/edges/turn_of_month.py, +2.2R/yr diversifier). BRICK3 = MACD(12,26,9)+RSI on BTC+ETH (arXiv 2206.12282), UPGRADED from breakout-20 (+25.5 vs +9.8 R/yr, RoMaD 25.3 vs 11.8). PORTFOLIO (equal 1R/trade, 2018+): corr all ~0 (NAS-gold .02, NAS-crypto .00, gold-crypto -.03), R/yr +37.9, maxDD 13.6R, RoMaD 23.9, combined Sharpe 2.56. @1R=0.5%: CAGR +20.3% maxDD 6.6% prop PASSED; @1R=0.85% recommended (daily -5% rule: worst day -4.33% @1%, 0 breaches, breach only >1.16%/trade). Coin set = BTC+ETH only (user-verified more DD-efficient than 4-coin). CAVEATS: all IN-SAMPLE candidates (not forward-tested); crypto=Yahoo spot + prop-tradability TBD; MACD/breakout are same crypto-trend family (brick3 is that single edge, not a 4th). No decorrelated 4th brick found across 9000 papers (constraint = data/options, not paper count). NEXT: forward-test.

<!-- NEXT-ACTIONS:START -->
## Next Actions

_This block is hand-editable and preserved across regenerations. Put the current
to-do list here._

- [ ] **The wall:** breadth works (exp-004, IR +0.58) but the prop universe is too narrow (exp-003, ~11 names, nothing significant). Decide how to get breadth *inside* the tradable universe.
- [ ] Rerun exp-004 on a survivorship-bias-free equity panel to de-inflate the +3.48%/yr.
- [ ] Pin `fx_loader.spreads_bps()` snapshot so exp-003's universe (10 vs 13 names) is reproducible.
- [ ] Consider widening exp-003 with indices/energy (needs an "index cash" session profile) to raise N.
<!-- NEXT-ACTIONS:END -->
